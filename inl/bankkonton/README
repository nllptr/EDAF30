Some notes:

- The bank uses single word strings as account owner id.
  Typically, a Swedish person number would be used, but is not enforced.
- Account numbers are integers starting at 1000000. There is no upper limit,
  so there is the possibility of overflowing the integer type.
